import Countdown from 'countdown.js';

// const initCountdown = () => {

const efcc_countdown = new Countdown({
  target: '.countdown',
  dayWord: ' days',
  hourWord: ' hours',
  minWord: ' mins',
  secWord: ' secs'
});

// }

// export { initCountdown }

