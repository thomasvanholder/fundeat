<% if user_signed_in? && current_user.owner == true %>
  <p>Internal note: You are an owner</p>
  <%= render "campaigns/shared/showowner" %>
<% else %>
  <%= render "campaigns/shared/investor" %>
<% end %>
