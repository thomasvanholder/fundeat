
<% if user_signed_in? && current_user.owner == false %>
  <%= render "investments/shared/sidemenu2" %>
  <%#= render "devise/shared/links" %>

<% else %>
  <%= render "Sign up as a investor now!" %>
<% end %>
